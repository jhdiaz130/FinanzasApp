<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fondo blanco -->
    <solid android:color="#FFFFFF"/>

    <!-- Bordes redondeados -->
    <corners android:radius="12dp"/>

    <!-- CONTORNO (esto es lo nuevo) -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD"/>

</shape>